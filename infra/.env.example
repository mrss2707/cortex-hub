# Cortex Hub — Server Environment
# Copy to .env and fill in actual values

# ── OpenAI (for mem0 embeddings) ─────────────
OPENAI_API_KEY=sk-xxx

# ── Neo4j ────────────────────────────────────
NEO4J_PASSWORD=cortex2026

# ── Dashboard API ────────────────────────────
PORT=4000
NODE_ENV=production
DATABASE_PATH=/app/data/cortex.db
